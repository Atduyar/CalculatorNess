import 'package:flutter/material.dart';

class CalculatorRoundedButton extends StatelessWidget {
  const CalculatorRoundedButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
